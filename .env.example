# Checkee source access is intentionally disabled until explicit authorization.
CHECKEE_ACCESS_MODE=disabled
# The default browser dataset is the sanitized, non-live static snapshot.
DATASET_MODE=checkee-static
NEXT_PUBLIC_DATASET_MODE=checkee-static
